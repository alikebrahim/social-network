import React from "react";
import { Typography } from "antd";

const { Title } = Typography;

const HomePage = () => {
  return <Title level={2}>Welcome to the Homepage!</Title>;
};

export default HomePage;
